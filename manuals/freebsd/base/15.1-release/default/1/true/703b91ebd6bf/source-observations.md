# 原文の識別

FreeBSD 公式リポジトリ `freebsd/freebsd-src` のコミット `5cbb1e05086c2cb510a9b77a6979dfb42c0cf215` にある `usr.bin/true/true.1` を取得しました。取得した roff のヘッダーコメントを含む全バイトを `source.roff` に保存しています。

取得時の GitHub API で、`releng/15.1` の先端は `88e7371d9dc26f85dfc1b008cbe59ebc7e4a33da` でした。取得元コミットとこの先端の比較では、先端が 415 コミット先、後れは 0、共通祖先は取得元コミットそのものでした。したがって、本資料は `releng/15.1` に含まれる上記の固定リビジョンとして識別しています。ブランチの最新状態を意味するものではありません。

原文の文書日付は `.Dd June 9, 1993` です。本文は外部ファイルを参照しない roff で、`mandoc` による HTML 化から IR・翻訳依頼・日本語訳まで、リポジトリの通常の処理を通しています。

roff の `.Xr` から生成される関連マニュアル参照には、原文側で URL が指定されていません。`builtin(1)`、`csh(1)`、`false(1)`、`sh(1)` は保護された参照テキストとして表示し、取得元の `true.1` へのリンクを補っていません。
